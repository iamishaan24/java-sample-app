pipeline{
      agent any

      parameters{
               string(name: 'NAME', defaultValue: 'Ishaan', description: 'enter name' )
                }
      stages{
          stage('one'){
              steps{
                  echo 'Hello! ${params.NAME}'
              }
          }
      }

}
